"""Schema fragments shared by skill ``Args`` models — and nothing else.

``_shared`` holds no logic and no task: it has no ``task.yml``, so discovery
walks past it. See :mod:`poseidon.tasks._shared.fragments`.
"""
