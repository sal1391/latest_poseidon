# Live-Synthesis Grounding + Settings-Surface Fixes

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Two tasks, sequential. Root-cause evidence: `.superpowers/sdd/2026-08-05-live-synthesis-fix/investigation-report.md` (read before Task A).

**Goal:** Ground live-mode answer narratives in the real query results (walkthrough findings F1+F4), and land Carlos's settings-surface decisions (F5+F6) plus the dev|local test-pollution fix (F2). F3 (parser widening for lowercase cue-less customer mentions) is explicitly OUT of scope — deferred with rationale below.

## Global Constraints

- **Model policy (Carlos's directive, unchanged):** the controller orchestrates and reviews only, never writes code. Task A: **opus** implementer + **opus** reviewer (cross-seam prompt/loop work with live verification). Task B: **sonnet** implementer + **sonnet** reviewer. Final review: **opus** over the whole fix range.
- **Root cause being fixed (Task A), verbatim from the investigation:** `loop.py:464-488` feeds the model a three-line digest (`"table(rows=5)"` + proof line) as the ENTIRE `toolResult` content — the real rows reach the model by no channel at all; `router/system.md` contains zero grounding rules; the state block's `Pass-through:` line leaks the PREVIOUS turn's result names into the prompt, which the model borrows when fabricating. Fix = (A) capped real rows in the toolResult content + (B) grounding rules in the prompt + (C) relabel the pass-through line — **A and B ship together in one task; B alone makes fabrication worse** (the investigation's own finding).
- **F3 is deferred, deliberately.** The live behavior (model asks a clarifying question) is acceptable interim UX and strictly better than what the deterministic path would have done (fuzzy auto-resolve silently picks Meridian Bunkering among several Meridians — a wrong answer dressed as right). Parser widening needs an ambiguity-guard design of its own; parked to the pile with the investigation's counterfactual warning attached. Do not partially fix it in Task A.
- **F6's split (Carlos approved the UI removal; this plan states the split for his sign-off):** the Version History section and Restore buttons leave the settings panel entirely; the BACKEND keeps append-only versioning, the `/api/me/memory/versions*` routes, and their tests untouched (invisible audit/undo — support can restore via API; deleting the machinery buys nothing and costs a migration). Frontend-only demolition.
- **Prompt versioning:** `router/system.md` carries a `{# version: v1 -#}` marker recorded per-call as `llm_calls.prompt_version`. Task A bumps it to `v2` — provenance keeps old and new runs distinguishable. Any byte-pinned prompt tests are updated in the same task, disclosed in the report (they are sanctioned below — do not treat pin updates as scope creep).
- **Live verification is part of Task A's definition of done** (authorized by Carlos for this fix): flip live via the untracked override (`docker compose -f infra/docker-compose.yml -f infra/docker-compose.live.yml up -d`), re-run the five-prompt gate scenario (fresh conversation, same shapes: flagship query → "what about March?" → mode/metric switches → Rotterdam), verify every narrative's customers/values appear in that turn's OWN table part and no prose tables are emitted, capture the transcript evidence in the report, then REVERT to stub (`docker compose -f infra/docker-compose.yml up -d`). Keep live calls to the one scenario (±1 retry). Never commit the override file; never read `backend/.env` values.
- **ENVIRONMENT:** withhold `PERPLEXITY_API_KEY` on every backend run; Windows venv `backend/.venv/Scripts/python.exe`; pg needs `DATABASE_URL=postgresql+psycopg://poseidon:poseidon@localhost:5432/poseidon`; ASCII source; ruff/tsc/oxlint clean on touched files; conventional commits on `phase-3-8-overnight`, trailer naming the ACTUAL implementer model (e.g. `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`); NEVER push; TDD RED-first with captured evidence.
- Baselines: offline 1600/12 skip, pg 320/1 skip, vitest 171 (implementers re-pin from real runs).
- **Sanctioned modifications to closed files:** `core/llm/loop.py` (ONLY the toolResult-content assembly at the dispatch-feedback site); `config/prompts/router/system.md` (grounding rules + version bump); `core/llm/prompts.py` (ONLY `render_state_block`'s pass-through-line relabel); `backend/tests/test_llm_loop.py` + any test byte-pinning the prompt or state block (pin updates for v2 + new grounding tests); `backend/tests/test_live_chat_sse.py` (F2: run-unique `X-Dev-User` + the false docstring claim); `frontend/src/features/settings/SettingsPanel.tsx` + `frontend/src/state/settingsStore.ts` + their test files (+ `mocks/handlers.ts` if handlers for versions become unused) (F5/F6); `docs/architecture/01-frontend.md` (ONLY the §9 settings-surface amendment recording Carlos's F6 decision). Everything else untouched. `api/client.ts`/`api/types.ts` keep their version functions/DTOs (the API remains; unused-export lint, if any, is the implementer's to resolve minimally and disclose).

### Task A (opus): ground the narrative in the real result — F1 + F4 + the pass-through leak

**Steps:** RED: offline tests proving (1) the toolResult content block sent back to the model contains the dispatched query's REAL rows (assert on captured request messages via the established recording-provider pattern — rows capped: full result up to 50 rows / ~4KB, then an explicit truncation marker; every gate-scenario result is top-5, far under cap), (2) the v2 prompt text contains the grounding rules, (3) `render_state_block`'s carry-over line no longer reads `Pass-through:` but an explicitly context-only label (e.g. `Carried context (prior turns, NOT current results):`) — pins updated. GREEN: implement (A) rows-in-toolResult, (B) `router/system.md` v2 grounding section — the narrative may only use values present in this turn's toolResult; the table/proof are ALREADY rendered to the user — reference them, never reproduce them; NEVER emit markdown tables in prose (F4); if the result is empty/failed, say so rather than invent — (C) the relabel. Full offline+pg. **Live verification per Global Constraints, evidence in report.** Commit.

### Task B (sonnet): F2 + F5 + F6

**Steps:** RED-first per item. **F2:** `test_live_chat_sse.py`'s injection test(s) switch to a run-unique `X-Dev-User` (the file's false "no way to seed a different user" docstring corrected — `test_me_routes.py` is the in-repo precedent); sweep the file for any other header-less writes (symmetric-pair discipline); prove post-fix via a count query in the report: a full pg run adds ZERO `user_memory`/`memory_outbox` rows for `dev|local`. **F5:** Save buttons (instruction AND entries — both halves of the pair) disabled unless their dirty flag is set; vitest for both. **F6:** remove the Version History section + Restore buttons + the store's versions/restore slices and actions (UI layer only; API/client functions/backend stay); `saveMemoryEntries` drops its now-moot `loadVersions` refresh; tests of removed UI removed, tests of surviving behavior updated. **Doc:** amend `docs/architecture/01-frontend.md` §9 — settings surface = instruction + memory entries with delete/save; version history removed per owner decision 2026-08-05; backend versioning retained as invisible audit/undo. vitest + tsc + oxlint + full backend suites. Commit.

**Task B amendment (2026-08-05, controller, same Carlos-approved pattern as the P13 sanction-line gaps):** Task B's own full-suite F2 proof exposed a second `dev|local` polluter outside its sanctioned files: `backend/tests/test_chat_e2e_scripted.py` (shares the `read_sse` helper; a full pg run still adds +8 `memory_outbox` rows for `dev|local`, that file a confirmed contributor). Sanctioned additions: `backend/tests/test_chat_e2e_scripted.py` (the same run-unique `X-Dev-User` conversion + sweep — and after it, re-run the full-suite before/after proof and chase the `memory_outbox` delta to ZERO; if any residue traces to yet another file, disclose it rather than fix it); `docs/architecture/01-frontend.md` §11 (ONLY the now-stale "version restore" phrase). The implementer's disclosed minimal edit to `frontend/src/features/auth/UserMenu.test.tsx` (dropping a `loadVersions` stub that no longer compiles after F6's store change) is ratified in scope — compile-necessity, no behavior.

**Pre-final wave (2026-08-05, controller-ruled carries from Task A's review):** (1) `config/prompts/router/system.md` — soften ONLY rule 3's closing "listing them all is not [fine]" clause to guidance ("prefer summarizing or highlighting over re-listing every row") — the live evidence showed grounded-but-complete prose restatements, which are harmless; the hard NEVER on markdown/pipe tables stays. Bump the version marker `v2`→`v3` (honest provenance for a content change) and update every pin the bump/clause touches (`test_llm_prompts.py`, `test_chat_e2e_scripted.py`, any grounding-section pin in `test_llm_loop.py`). (2) `docs/architecture/03-llm-routing.md` — ONLY the §(around line 109-110) "bulk rows/artifacts are stripped from LLM-visible tool results (digest + reference only)" sentence, updated to state the reversed rule (capped real rows in the toolResult content since this fix; digest-only was the pre-fix design). Nothing else in either file.

### Final review (opus)

Whole fix range, both tasks + the pre-final wave; verifies Task A's live evidence is genuine and Task B's demolition left no dead wiring; triages any new minors and the ledger's deferred/ruled items.

## Gate (Carlos)

1. Suites green. 2. Live: the five-prompt scenario re-run — narratives use only real values, no prose tables (Task A's captured evidence, spot-check in browser if desired). 3. Settings panel: no Version History, Save disabled until an edit exists. 4. One full pg run leaves `dev|local`'s memory untouched.
